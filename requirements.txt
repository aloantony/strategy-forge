# --- Backend (broker-agnostico; MT5 es opcional) ---
MetaTrader5; sys_platform == "win32"
pandas
dukascopy-python>=4.0.1

# --- Servidor (API REST + WebSocket) ---
fastapi
uvicorn[standard]
httpx       # cliente HTTP (TestClient y ApiClient del frontend)
websockets  # cliente WS del frontend (frontend/desktop/api_client.py)

# --- Frontend desktop ---
matplotlib
customtkinter
lightweight-charts
